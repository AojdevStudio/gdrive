# Composio-Native Issue Backlog

GitHub issue updates were planned for issues `#80` through `#84`, but live issue edits require a GitHub identity with `UpdateIssue` permission on `AojdevStudio/gdrive`.

Use the following replacements when authenticated with a permitted account.

## #80 - Record Composio as AOJ Workbench native provider

> *This was generated by AI during triage.*

### What to build

Record the corrected AOJ Workbench provider decision: Composio SDK is the native provider layer, and the direct Google Workspace implementation is legacy migration scaffolding. This replaces the older companion-MCP framing.

### Acceptance criteria

- [ ] `CONTEXT.md` defines AOJ Workbench as the single agent-facing knowledge-work MCP surface.
- [ ] The Composio guide describes Composio SDK as the native provider layer inside AOJ Workbench, not a separate client-facing MCP.
- [ ] ADR `0002` records Composio managed auth and Composio SDK sessions as the target provider model.
- [ ] The guide index links to the Composio native provider guide.
- [ ] Docs state Google Workspace is a provider toolkit behind Composio in the target architecture.
- [ ] Docs state the direct Google implementation is legacy migration scaffolding.
- [ ] No private account inventory, raw mailbox list, or provider credentials are added to the repo.

### Sources

- https://docs.composio.dev/docs/authentication
- https://docs.composio.dev/docs/tools-and-toolkits

### Blocked by

None - this is the foundation issue.

## #81 - Define Composio-native AOJ Workbench terminology

> *This was generated by AI during triage.*

### What to build

Update the project glossary so future agents use the Composio-native provider model precisely and do not reintroduce Google-native or companion-MCP language.

### Acceptance criteria

- [ ] `CONTEXT.md` defines `Composio native provider`.
- [ ] `CONTEXT.md` defines `Provider toolkit`.
- [ ] `CONTEXT.md` defines `Legacy Google provider`.
- [ ] `CONTEXT.md` defines `Provider replacement slice`.
- [ ] `CONTEXT.md` preserves connected-account/mailbox terminology without raw email addresses or private inventories.
- [ ] `CONTEXT.md` states provider authorization belongs to the provider layer, not MCP client auth.
- [ ] The glossary states permanent dual provider paths are not allowed after a replacement slice is green.

### Blocked by

- #80

## #82 - Document managed-auth BWS and env contract

> *This was generated by AI during triage.*

### What to build

Document the v1 secret/env contract for AOJ Workbench as a Composio-managed-auth private MCP. The goal is to avoid over-requesting provider OAuth credentials and keep provider setup in the Composio dashboard.

### Acceptance criteria

- [ ] `.env.example` lists `COMPOSIO_API_KEY` and `AOJ_WORKBENCH_USER_ID` as required Composio-native values.
- [ ] Environment docs state `AOJ_WORKBENCH_MCP_TOKEN` is the local client alias for `MCP_BEARER_TOKEN`, not a separate secret.
- [ ] Environment docs state `CLOUDFLARE_API_TOKEN` should be sourced from `AOJDEVSTUDIO_CLOUDFLARE_API_TOKEN` in BWS.
- [ ] Environment docs state provider OAuth client IDs/secrets are not needed for v1 managed auth.
- [ ] Environment docs state `COMPOSIO_WEBHOOK_SECRET` is not needed until trigger/webhook work begins.
- [ ] Legacy `GDRIVE_*` values are clearly marked as direct-Google migration scaffolding.

### Blocked by

- #80
- #81

## #83 - Design search/execute over Composio sessions

> *This was generated by AI during triage.*

### What to build

Define the AOJ Workbench MCP surface after Composio becomes native. The public MCP surface should stay small: agents call AOJ Workbench `search` and `execute`; AOJ Workbench wraps Composio session discovery, schema lookup, auth status, and execution behind those tools.

### Acceptance criteria

- [ ] Docs state AOJ Workbench keeps `search` and `execute` as the public MCP tools.
- [ ] Docs state `search` should wrap Composio session discovery/schema/auth-status behavior.
- [ ] Docs state `execute` should wrap Composio session execution behavior.
- [ ] Docs explicitly reject exposing every Composio toolkit action as a separate client-facing MCP tool for the target architecture.
- [ ] Docs state v1 adds no new AOJ Workbench runtime permission gate; preserve execute-style behavior and rely on Composio managed auth/scopes plus agent judgment.
- [ ] The implementation plan calls for behavior-level tests before each provider replacement slice.

### Sources

- https://docs.composio.dev/docs/tools-and-toolkits
- https://docs.composio.dev/docs/authentication

### Blocked by

- #80
- #81

## #84 - Plan first provider replacement slice and smoke test

> *This was generated by AI during triage.*

### What to build

Prepare the first ready-for-agent provider replacement slice. A replacement slice proves one legacy Google capability through the Composio native provider, gets green behavior-level verification, and removes the matching legacy direct-Google path.

This issue is planning/triage only. Do not implement provider code in this batch.

### Acceptance criteria

- [ ] Define the first vertical replacement slice using existing AOJ Workbench `search`/`execute` behavior as the public interface.
- [ ] Include a behavior-level test plan for the selected capability.
- [ ] Include a smoke-test procedure for Composio-backed discovery, auth status, and execution through AOJ Workbench.
- [ ] State that legacy direct-Google code must be removed after the Composio-backed behavior is green.
- [ ] State that permanent dual provider paths are not allowed.
- [ ] Do not publish private mailbox aliases, raw email addresses, or provider credentials.

### Blocked by

- #82
- #83
